# =============================================================================
# Vercel + Supabase - Production Environment Variables
# =============================================================================
# Configure estas variáveis no painel da Vercel (Settings > Environment Variables)
# =============================================================================

# =============================================================================
# SUPABASE DATABASE
# =============================================================================
# Connection string do Supabase PostgreSQL (use connection pooling para serverless)
# Formato: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-DB-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (use para migrations apenas)
DATABASE_URL_DIRECT="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-DB-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# =============================================================================
# SUPABASE STORAGE & AUTH
# =============================================================================
# URL do projeto Supabase (Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"

# Supabase anon/public key (Settings > API > Project API keys > anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# Supabase service role key (Settings > API > Project API keys > service_role)
# ⚠️ NUNCA exponha essa chave no frontend - apenas no backend
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Nome do bucket de storage para anexos (crie em Storage > Create bucket)
SUPABASE_STORAGE_BUCKET="pwr-attachments"

# =============================================================================
# JWT & AUTH
# =============================================================================
# Secret para tokens JWT (gere uma string segura aleatória)
JWT_SECRET="your-production-jwt-secret-change-this"
JWT_EXPIRES_IN="7d"

# =============================================================================
# API & FRONTEND
# =============================================================================
# URL do frontend (URL da Vercel após deploy)
FRONTEND_URL="https://your-app.vercel.app"

# URL da API (será a mesma URL da Vercel com /api prefix)
NEXT_PUBLIC_API_URL="https://your-app.vercel.app/api"

# =============================================================================
# OPTIONAL: REDIS (Upstash para filas)
# =============================================================================
# Se você quiser manter filas/cache, use Upstash Redis (serverless-friendly)
# REDIS_URL="rediss://default:[PASSWORD]@[HOST]:[PORT]"

# =============================================================================
# NODE ENVIRONMENT
# =============================================================================
NODE_ENV="production"
